<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.warp.killthedj"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="8"/>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application android:icon="@drawable/icon" android:label="@string/app_name" >

        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KillTheDjActivity"></activity>
        <activity android:name=".track.SearchTrackActivity"></activity>
        <activity android:name=".playlist.PlaylistActivity"></activity>
        <activity android:name=".myrequests.MyRequestsActivity"></activity>
        <activity android:name=".current.CurrentTrackActivity"></activity>

    </application>

</manifest>